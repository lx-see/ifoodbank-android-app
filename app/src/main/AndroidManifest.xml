<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.ifoodbank">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:hardwareAccelerated="false"
            android:icon="@mipmap/logo1"
            android:label="@string/app_name"
            android:largeHeap="true"
            android:roundIcon="@mipmap/logo2"
            android:supportsRtl="true"
            android:theme="@style/Theme.Ifoodbank">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="@string/google_maps_key"/>

        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".LoginActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".SignupActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".RequestFoodActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".ApplyFoodActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".GalleryActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".HotlineActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".AllLocationActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".ProfileActivity"
                android:exported="false"
                android:label="@string/app_name"/>
        <activity
                android:name=".UpdateProfileActivity"
                android:exported="false"
                android:label="@string/app_name"/>
        <activity
                android:name=".CalendarActivity"
                android:configChanges="uiMode"
                android:label="@string/app_name"/>
        <activity
                android:name=".NotificationActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".AcceptionHistoryActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".MapsActivity"
                android:label="@string/title_activity_maps"/>
        <activity
                android:name=".FBOwnerMainActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".FBOwnerLoginActivity"
                android:label="@string/app_name"/>
        <activity
                android:name=".FBOwnerSignupActivity"
                android:label="@string/app_name"/>
    </application>

</manifest>